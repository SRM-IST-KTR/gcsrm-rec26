# Backend URL for OTP + recruitment endpoints. Empty in dev -> Next.js
# rewrites proxy /api/otp/* and /api/recruitment/* to
# http://localhost:3000 (see next.config.ts). Set to your deployed backend URL
# in production.
NEXT_PUBLIC_API_URL=